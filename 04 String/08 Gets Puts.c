#include <stdio.h>
#include <string.h>
int main(){
        
            char str[50];
               
    // gets for input string 
    // puts for output string 
            gets(str);
            puts(str);

              
    return 0;
}